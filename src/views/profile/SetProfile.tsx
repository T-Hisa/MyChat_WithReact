import { Component } from "react"
import ProfileCommon from "../../components/profile/ProfileCommon"

import RouteProps from "../../types/RouteProps"

class SetProfile extends Component<RouteProps, {}> {
  render() {
    return (
      <ProfileCommon/>
    )
  }
}

export default SetProfile
